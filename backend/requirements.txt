# Backend inference environment, verified with Python 3.10.
# PyTorch itself is installed from the official CPU-only index in the
# Dockerfile.  Keep this separate from requirements_colab.txt: Colab retains
# its GPU-optimised PyTorch build for training.
torch-geometric==2.8.0.post1
pandas==2.3.3
numpy==1.26.4
scikit-learn==1.7.2
rdkit==2026.3.5
fastapi==0.128.8
uvicorn==0.52.3
pydantic==2.12.5
requests==2.32.5
